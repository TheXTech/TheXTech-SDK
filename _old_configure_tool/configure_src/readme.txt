To build it you need for MinGW compiller and it's "bin" directory must be in the PATH environment!

(Alternately you can use MSVC 2015)

Run the "build.bat" to build the configure application with MinGW.
Run the "build_msvc.bat" to build the configure application with MSVC 2015.
